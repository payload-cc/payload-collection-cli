// Shared constants used across the Payload app and CLI config.
// Demonstrates that CLI config files can import values from other project files.
export const DEFAULT_CATEGORY_NAME = "uncategorized";
